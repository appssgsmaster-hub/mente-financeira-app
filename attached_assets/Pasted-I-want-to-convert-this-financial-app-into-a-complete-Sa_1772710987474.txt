I want to convert this financial app into a complete SaaS platform with proper architecture, not patches or small fixes.

Please refactor the project to support a multi-user SaaS system with authentication, subscriptions, and trial management.

Requirements:

1. Authentication system
- Email + password login
- Secure password hashing
- Session management
- Signup, login, logout
- Protected routes (users must be logged in to access the app)

2. Database structure
Create proper database tables:
- users
- subscriptions
- accounts
- transactions
- plans

Each user must only see their own financial data.

3. Trial system
- New users get a 15-day free trial automatically
- Store trial_start_date and trial_end_date
- When trial expires, the system must restrict access until payment

4. Subscription system
Integrate Stripe for subscriptions:
- Create monthly plan
- Connect Stripe checkout
- Store stripe_customer_id
- Store stripe_subscription_id
- Update subscription status with webhooks

5. Billing flow
User flow must be:

Signup → 15 day free trial → trial expires → redirect to subscription page → payment via Stripe → access restored.

6. Security
- All API endpoints must validate the authenticated user
- Users cannot access other users' financial data

7. Refactor API routes
All financial routes must be user-scoped:
GET /api/accounts
GET /api/transactions
POST /api/transactions

Each request must use the logged in user's ID.

8. Frontend changes
Add:
- Login page
- Signup page
- Subscription page
- Trial expiration page

9. Environment variables
Prepare the system for production with:
- DATABASE_URL
- STRIPE_SECRET_KEY
- STRIPE_WEBHOOK_SECRET
- SESSION_SECRET

10. Keep existing financial logic
Do not break the current financial system (accounts, distribution, transactions). Only extend it to support users and subscriptions.

Important:
Please implement this with a clean architecture, not temporary fixes.
Create all necessary migrations, backend routes, and frontend pages.