We need to improve the app focusing on MOBILE USERS, because most users will access the app through their phones.

Please analyze the entire application and implement the following improvements in a clean and stable way without breaking existing functionality.

1. FIX MOBILE HISTORY ISSUE
Currently the transaction history (payments, income, or commitments) appears correctly on desktop but does not appear on mobile devices.

Fix this so that:
- The history loads correctly on mobile screens
- The same database data is shown on all devices
- Transactions added on desktop appear instantly on mobile
- Transactions added on mobile appear instantly on desktop
- Ensure the issue is not caused by responsive rendering, state loading, or API calls failing on small screens.

2. ENSURE CROSS-DEVICE SYNCHRONIZATION
Guarantee that all data is fully synchronized across devices.

Requirements:
- Any entry added on desktop must immediately reflect on mobile
- Any entry added on mobile must appear on desktop
- Use the existing database as the single source of truth
- Verify API calls and state refresh logic
- Ensure the dashboard, history, accounts and commitments update correctly.

3. MOBILE FRIENDLY DESIGN
Improve the mobile UI to make the app easy to use on phones.

Adjust:
- Responsive layout
- Cards stacking correctly
- Font sizes readable
- Buttons large enough for touch
- Prevent horizontal scroll
- Ensure charts and account cards render properly.

4. DOWNLOAD / EXPORT BUTTON (MOBILE INCLUDED)
Create a button called:

"Download Report"

This button must:
- Work on desktop and mobile
- Export the user's financial data
- Allow download in CSV or PDF
- Include transactions, accounts and balances.

5. APP ICON AND BRAND IMAGES
Create placeholders for three images for the application:

1. App Icon (for mobile / browser)
2. App Logo (dashboard header)
3. Social preview image (when sharing the link)

Use clean placeholders with the name:

"Mente Financeira – Prosperar é Viver"

Do not use heavy files. Use simple SVG or lightweight images.

6. PERFORMANCE CHECK
After implementing the changes:
- Verify that the app works correctly on desktop and mobile
- Ensure no console errors
- Ensure the database reads/writes correctly
- Ensure deployment remains stable.

Important:
Do not remove existing functionality.
Make improvements in a structured and maintainable way.