IMPORTANT: UI CHANGE ONLY — DO NOT MODIFY ANY EXISTING LOGIC, CALCULATIONS, DATABASE STRUCTURE, OR BUSINESS RULES.

The current “Histórico de Transações” mixes income and expenses in a single list. This must be changed ONLY at the UI level.

TASK:
Split the existing transaction history into TWO separate visual cards:

1) “Histórico de Entradas”
- Display ONLY transactions where type = income
- Keep ALL existing transaction fields exactly as they are (date, description, category/source, amount, edit, delete)
- Keep existing styles for income (green color, positive values)
- Show total number of income transactions
- Use the same transaction component currently in use (DO NOT rewrite it)

2) “Histórico de Saídas”
- Display ONLY transactions where type = expense
- Keep ALL existing transaction fields exactly as they are (date, description, category/account, amount, edit, delete)
- Keep existing styles for expense (red color, negative values)
- Show total number of expense transactions
- Use the same transaction component currently in use (DO NOT rewrite it)

LAYOUT:
- Desktop: both cards must appear side by side (2 columns, equal width)
- Mobile: stack vertically (Entradas first, Saídas below)
- Maintain existing design system (spacing, fonts, colors, borders, shadows, icons)
- Each card must have its own scroll if content overflows

STRICT RULES:
- DO NOT change how transactions are created, edited, deleted, or stored
- DO NOT change any calculations or balances
- DO NOT duplicate data
- DO NOT create new transaction structures
- ONLY filter existing transactions into two lists based on type

REACTIVITY (CRITICAL):
- When a new income is created → it must instantly appear in “Histórico de Entradas”
- When a new expense is created → it must instantly appear in “Histórico de Saídas”
- When editing a transaction and changing its type → it must automatically move to the correct card
- When deleting → it must disappear from the correct card only

GOAL:
Improve readability by separating income and expenses visually, without affecting any existing functionality.